<header class="global-header">
  <div class="header-container">
    <div class="header-logo">
      <%= link_to root_path, class: "logo-link" do %>
        <span class="logo-text"><%= t('app.title') %></span>
      <% end %>
      <%= link_to t('app.menu.stages'), stages_path, class: "stages-link" %>
      <%= link_to t('app.menu.multiplay'), multiplay_recruitments_path, class: "stages-link" %>
    </div>

    <nav class="header-nav">
      <!-- 言語切り替えメニュー -->
      <div class="language-menu-container">
        <button class="language-menu-trigger" popovertarget="language-menu-popover">
          <span class="language-icon">🌍</span>
          <span class="language-code"><%= I18n.locale.to_s.upcase %></span>
        </button>

        <div id="language-menu-popover" popover class="language-menu-popover">
          <% I18n.available_locales.sort.each do |locale| %>
            <%= link_to url_for(locale: locale), class: "language-menu-item #{'active' if I18n.locale == locale}" do %>
              <span class="language-name"><%= t("languages.#{locale}") %></span>
              <span class="language-code-small"><%= locale.to_s.upcase %></span>
            <% end %>
          <% end %>
        </div>
      </div>

      <% if logged_in? %>
        <div class="user-menu-container">
          <button class="user-menu-trigger" popovertarget="user-menu-popover">
            <span class="user-menu-name"><%= current_user.display_name %></span><%= t('app.common.suffix_san') %>
            <% if current_user.admin? %>
              <span class="admin-badge"><%= t('app.common.admin_badge') %></span>
            <% end %>
          </button>

          <div id="user-menu-popover" popover class="user-menu-popover">
            <%= link_to user_path(current_user), class: "user-menu-item" do %>
              <svg class="user-menu-item-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A13.937 13.937 0 0112 16c2.5 0 4.847.655 6.879 1.804M15 10a3 3 0 11-6 0 3 3 0 016 0zm6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <%= t('app.menu.my_page') %>
            <% end %>
            <%= link_to profile_path, class: "user-menu-item" do %>
              <svg class="user-menu-item-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
              </svg>
              <%= t('app.menu.account_settings') %>
            <% end %>
            <%= button_to logout_path, method: :delete, class: "user-menu-item user-menu-item-logout" do %>
              <svg class="user-menu-item-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
              </svg>
              <%= t('app.menu.logout') %>
            <% end %>
          </div>
        </div>
      <% else %>
        <div class="header-actions">
          <%= link_to t('app.menu.login'), login_path, class: "btn btn-nav" %>
          <%= link_to t('app.menu.signup'), signup_path, class: "btn btn-nav btn-primary" %>
        </div>
      <% end %>
    </nav>
  </div>
</header>

